# AI Trading Bot Research Society — V27

Production application lives under `app/`. Render/Docker starts `app.main:app`.

V27 makes the AI response authoritative for final comment wording when it passes source/domain safety; deterministic composition is fallback-only.

See `docs/STRUCTURE_V27.md` and `README_V27_AI_FIRST_PIPELINE.md`.
